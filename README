libdiscid Python bindings
-------------------------

python-libdiscid implements Python bindings for libdiscid. Both Python 2.x and
3.x are supported.

Dependencies
~~~~~~~~~~~~

libdiscid
cython

Usage
~~~~~

::

    from libdiscid import DiscId

    disc = DiscId()
    disc.read("/dev/cdrom")
    print "id: %s" % (disc.id, )

License
~~~~~~~

python-libdiscid is release under the Expat license. Check LICENSE for details.
